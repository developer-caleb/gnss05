<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="kr.loplab.gnss05">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gnss05">
        <activity android:name=".FileExportActivity"></activity>
        <activity android:name=".ColorPickerdialog" />
        <activity android:name=".NaverMap" />

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="x3tscpw23x" />

        <activity
            android:name=".StandardPointActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>